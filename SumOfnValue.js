function sumValues(n)
{
    let sum=0
    for(let i=1; i<=n ; i++)
    {
        sum +=i
            console.log(`${i} and the current ${sum} `)

    
}
        return sum
}
       const result= sumValues(5)
console.log(`Final Sum between 1 and 5 = ${result}`);
